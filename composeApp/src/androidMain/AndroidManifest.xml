<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- [#39] NIP-55: Android 11+ のパッケージ可視性。外部署名アプリ(Amber 等)を
         検出・起動し、ContentResolver で署名/暗号を委譲するために nostrsigner: を宣言する。 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="nostrsigner" />
        </intent>
    </queries>

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <!-- フォルダブル/大画面対応: リサイズ可能にして折り↔展開で幅が変わるようにする -->
        <!-- [#100][#101] singleTask: 共有/ディープリンクで既存タスクを再利用し、
             起動中は onNewIntent に Intent が届くようにする（多重起動を防ぐ）。 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:resizeableActivity="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- [#100] 共有ターゲット: 他アプリの共有シートからテキストを受け取り投稿コンポーザーへ -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <!-- [#101] nostr: ディープリンク（npub/nprofile/note/nevent） -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="nostr" />
            </intent-filter>
        </activity>
    </application>
</manifest>
